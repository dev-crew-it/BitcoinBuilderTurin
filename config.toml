base_url = "https://bitcoinbuilderturin.xyz"
title = "runin"
description = "runin — Bitcoin Builder Turin: a monthly meetup on product, design, and AI tools for building on bitcoin."
compile_sass = false
build_search_index = false
generate_feeds = true
feed_filenames = ["feed.xml"]

[markdown]
# Code highlighting is off by default in Zola; the old `highlight_code = false`
# field was removed in newer versions, so no setting is needed here.

[extra]
